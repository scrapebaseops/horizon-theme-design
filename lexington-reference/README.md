# Lexington reference (read-only)

Copy or clone the **purchased Lexington theme** into this folder for visual reference.

Agents should **read** this codebase to extract design language; **edits** belong under **`horizon-themes/claude/`**, **`openai/`**, or **`gemini/`** (whichever variant is active) unless you explicitly allow reference changes (e.g. benchmark templates there).

See `docs/PROJECT_CONTEXT.md`.
