# Lexington reference (read-only)

Copy or clone the **purchased Lexington theme** into this folder for visual reference.

Agents should **read** this codebase to extract design language; **edits** belong in `horizon-theme/` unless you explicitly allow reference changes (e.g. benchmark templates).

See `docs/PROJECT_CONTEXT.md`.
